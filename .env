# HyRL Server Configuration

# Logging configuration
DEBUG=false

# Server configuration
HOST=0.0.0.0
PORT=50051

# Model configuration
MODEL_PATH=src/hyrl/models
USE_HYBRID_MODELS=true

# Environment configuration
# WARNING: Modifying these bounds requires retraining the models
# The current models are trained specifically for these dimensions
BOUNDS_X_MIN=0.0
BOUNDS_X_MAX=3.0
BOUNDS_Y_MIN=-1.5
BOUNDS_Y_MAX=1.5

# Obstacle configuration
OBSTACLE_CENTER_X=1.5
OBSTACLE_CENTER_Y=0.0
OBSTACLE_RADIUS=0.75

# Goal configuration
# WARNING: Modifying the goal position requires retraining the models
# The current models are trained specifically for this goal location
GOAL_X=3.0
GOAL_Y=0.0

# Critical points configuration
CRITICAL_POINTS_COUNT=30
